---
title: "docker-agent-tail"
description: "Auto-discover Docker containers, tail their logs in real-time, and write structured files optimized for AI coding agents."
---

<img
  className="block dark:hidden"
  src="/logo-light.svg"
  alt="docker-agent-tail"
  noZoom
/>
<img
  className="hidden dark:block"
  src="/logo.svg"
  alt="docker-agent-tail"
  noZoom
/>

## Docker logs for AI agents

Auto-discover Docker containers, tail their logs in real-time, and write structured files optimized for AI coding agents like Claude Code and Cursor.

<CardGroup cols={2}>
  <Card title="Get Started" icon="rocket" href="/quickstart">
    5-minute tutorial to get up and running
  </Card>
  <Card title="Installation" icon="download" href="/installation">
    Install via Homebrew, Go, curl, or Docker
  </Card>
  <Card title="CLI Reference" icon="terminal" href="/cli-reference">
    Complete flag and option reference
  </Card>
  <Card title="AI Agents" icon="robot" href="/agents">
    Integrate with Claude Code, Cursor, and Windsurf
  </Card>
</CardGroup>

## Key Features

<CardGroup cols={2}>
  <Card title="Auto-Discovery" icon="magnifying-glass">
    Automatically detects all running Docker containers without manual configuration.
  </Card>
  <Card title="Real-Time Streaming" icon="bolt">
    Tail logs as they happen with configurable filtering and output formatting.
  </Card>
  <Card title="AI-Optimized Output" icon="file-lines">
    Structured log files with timestamps and container names perfect for AI analysis.
  </Card>
  <Card title="Docker Compose Support" icon="cubes">
    Works seamlessly with Docker Compose projects and services.
  </Card>
  <Card title="Flexible Filtering" icon="filter">
    Filter logs by regex patterns, exclude noisy containers, and mute specific streams.
  </Card>
  <Card title="Multiple Output Formats" icon="code">
    JSON output, combined logs, per-container logs, or raw stream mode.
  </Card>
</CardGroup>

## Quick Install

<Tabs>
  <Tab title="Homebrew">
    ```bash
    brew install docker-agent-tail
    ```
  </Tab>
  <Tab title="Go Install">
    ```bash
    go install github.com/scotello/docker-agent-tail@latest
    ```
  </Tab>
  <Tab title="Curl">
    ```bash
    curl -sSL https://github.com/scotello/docker-agent-tail/releases/latest/download/install.sh | bash
    ```
  </Tab>
  <Tab title="Docker">
    ```bash
    docker run --rm -v /var/run/docker.sock:/var/run/docker.sock scotello/docker-agent-tail
    ```
  </Tab>
</Tabs>

## Basic Usage

```bash
# Tail all container logs in real-time
docker-agent-tail --all --follow

# Tail specific containers
docker-agent-tail --names api,web --follow

# Filter logs with regex
docker-agent-tail --all --exclude 'health' --mute 'DEBUG'

# Output combined logs to a file
docker-agent-tail --all --output logs/combined.log
```

## Ready to get started?

Read the full getting started guide to learn how to use docker-agent-tail with AI agents.

<Card title="Getting Started Guide" icon="arrow-right" href="/quickstart">
  Follow the 5-minute tutorial
</Card>
